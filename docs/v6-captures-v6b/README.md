# v6.4 captures — CEO visual identity, final (2026-09-13)

One commit, five hero-surface items (DESIGN-SYSTEM.md v6.4 addendum is binding):

1. **HERO B** (CEO: "A looks broken"): giant number = live ms/block (validator perf
   samples, 3–4 chars + `ms` unit, fit floor 14px); ring stays the centerpiece; KPI
   matrix on the fold edge (Throughput · Finality 3 ticks · Bridged · Height, one shared
   poll); COOK price demoted to the plain-text masthead chip (exact USD, glyph law).
2. **Thick band** 0.17 × ring size; locked bite constants kept (the ±10% experiment
   collapsed the wound to one arc — see addendum); re-validated REAL 7/10 × 3/3.
3. **Accent roll**: C1/C2/C3 (`roll/` folder) vision-checked — C3 `#CE8A3C` won both
   order-swapped runs; text variants #94632B light / #E8B47A (toffee) dark.
4. **Stuffing**: mottle + 9 chips + 22 sugar specks on the thick band; COOKIE-ILLUSTRATION
   (not photo) 2/2, glance YES.
5. **Pill cut**: 3 pill affordances survive (MAINNET LIVE, toggle, hero CTA).

## Files
- `desktop-1440x900-{light,dark}.png`, `mobile-390x844-{light,dark}.png`,
  `desktop-1440-fullpage-light.png` — the capture set (console zero on every load).
- `ring-crop-{light,dark}.png` — 2× close-ups of the stuffed ring for CEO review.
- `roll/light-hero-{C1,C2,C3}.png` — the de-pumpkin roll candidates (token override).
- `gate-report.json` — full battery: contrast 22×2 (min 4.67 light / 6.5 dark),
  overflow ladder incl. **80px zoom-400 PASS (sw=80)**, keep-out proofs, reduced-motion,
  keyboard, cold-load audits, error state, tnum.

Release-gate ledger re-run: `../v6-captures-browsers/gate/verifier/report-rerun.json`
(zoom-400 fail→PASS; a11y carried 41; subjective rows HOLD-for-human).
